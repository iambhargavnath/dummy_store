name: dummy_store
description: "This is a Dummy Store App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_bloc: ^8.1.5
  dio: ^5.5.0+1
  get_it: ^7.7.0
  drift: ^2.18.0
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  path: ^1.9.0
  infinite_scroll_pagination: ^4.1.0
  cached_network_image: ^3.4.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.13
  drift_dev: ^2.22.1

